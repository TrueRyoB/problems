# Euclidean Distance

1. Prove that there exists only 1 centroid for an arbitrary graph on n-dimensional space (n>=1)
2. Design a code that, given $N$ nodes, solves its Euclidean distance to a certain node by $O(\log N)$
